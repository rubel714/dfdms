import React from 'react';
import './Spinner.styles.css'; // Import your CSS for styling the spinner

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
